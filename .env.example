ANTHROPIC_API_KEY=your-key-here

# TimescaleDB connection (wired starting Phase 4 — the hot path runs in-memory until then)
TIMESCALE_DSN=postgresql://postgres:postgres@localhost:5432/control_loop_sim
