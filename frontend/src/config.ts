// 9a's backend, local-only -- no env-based config yet, matches this
// project's "local-only, like the rest of the project" scope for Phase 9.
export const API_BASE_URL = "http://localhost:8000";
export const WS_URL = "ws://localhost:8000/ws/ticks";
